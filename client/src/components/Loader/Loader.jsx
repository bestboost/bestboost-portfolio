import React from "react";
import { SpinnerWrapper, Spinner } from "./Loader.styled";

const Loader = () => {
  return (
    <SpinnerWrapper>
      <Spinner />
    </SpinnerWrapper>
  );
};

export default Loader;
